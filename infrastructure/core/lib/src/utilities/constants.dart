import 'package:flutter/foundation.dart';

const kDebugEmail = kDebugMode ? 'dev@crewnew.com' : '';
const kDebugPassword = kDebugMode ? '12345678' : '';
// TODO: Change the API Domain
const kApiEndpointUrl =
    kDebugMode ? 'http://localhost:1337' : 'https://api.domain.com';
