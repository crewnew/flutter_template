import 'package:flutter/material.dart';

class AppTheme {
  static ThemeData get light {
    return ThemeData.light().copyWith(
        // TODO: Add your own theme data
        );
  }

  static ThemeData get dark {
    return ThemeData.dark().copyWith();
  }
}
